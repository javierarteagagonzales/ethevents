---
import Button from '../ui/Button.astro';
import { Download, Plus } from 'lucide-astro';
---

<div
  id="members-section"
  class="dashboard-section hidden bg-surface border border-white/5 rounded-3xl p-6 md:p-8 backdrop-blur-xl flex flex-col gap-6"
>
  <!-- TABLE TOP CONTROLS -->
  <div
    class="flex flex-col sm:flex-row justify-between items-center gap-4"
  >
    <div class="w-full sm:max-w-xs relative">
      <h2 class="text-xl font-extrabold text-white">
        Miembros del Equipo
      </h2>
    </div>

    <div class="flex flex-wrap gap-3 w-full sm:w-auto">
      <Button id="btn-download-members-json" variant="primary" class="flex-1 sm:flex-none">
        <Download class="w-4 h-4" />
        MEMBERS.JSON
      </Button>
      <Button id="btn-new-member" variant="primary" class="flex-1 sm:flex-none bg-accent-2 text-[#030407]">
        <Plus class="w-4 h-4" />
        AGREGAR MIEMBRO
      </Button>
    </div>
  </div>

  <!-- TABLE -->
  <div
    class="overflow-x-auto rounded-2xl border border-white/5 bg-black/20"
  >
    <table class="w-full border-collapse text-left text-xs text-muted">
      <thead
        class="bg-surface-2 font-mono text-[10px] tracking-wider uppercase text-content-secondary font-bold border-b border-white/5"
      >
        <tr>
          <th class="p-4">ID</th>
          <th class="p-4">Foto</th>
          <th class="p-4">Nombre</th>
          <th class="p-4">Cargo</th>
          <th class="p-4">Eventos</th>
          <th class="p-4 text-right">Acciones</th>
        </tr>
      </thead>
      <tbody id="members-table-body" class="divide-y divide-white/5">
        <!-- Dynamically populated rows -->
      </tbody>
    </table>
  </div>
</div>
